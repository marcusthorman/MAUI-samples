namespace localization_demo;

public partial class AppShell : Shell
{
    public AppShell()
    {
        InitializeComponent();
        //Routing.RegisterRoute(nameof(ChooseLanguagePage), typeof(ChooseLanguagePage));
        //Routing.RegisterRoute(nameof(MainPage), typeof(MainPage));
    }
}
